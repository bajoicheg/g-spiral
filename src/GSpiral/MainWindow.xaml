<Window x:Class="GSpiral.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:GSpiral.Presentation"
        xmlns:controls="clr-namespace:GSpiral.Controls"
        mc:Ignorable="d"
        Title="G-Spiral — Корпоративная культура"
        Width="1200" Height="800" MinWidth="900" MinHeight="650"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI"
        Background="#F7F8FA">
    <Window.Resources>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#26364A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="22,11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="7" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.45"/></Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#E7EBF0"/>
            <Setter Property="Foreground" Value="#26364A"/>
        </Style>
        <Style x:Key="ScreenStart" TargetType="Grid">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers><DataTrigger Binding="{Binding Screen}" Value="{x:Static vm:AppScreen.Start}"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers>
        </Style>
        <Style x:Key="ScreenQuestion" TargetType="Grid">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers><DataTrigger Binding="{Binding Screen}" Value="{x:Static vm:AppScreen.Question}"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers>
        </Style>
        <Style x:Key="ScreenResults" TargetType="Grid">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers><DataTrigger Binding="{Binding Screen}" Value="{x:Static vm:AppScreen.Results}"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers>
        </Style>
        <Style x:Key="ProgressSegment" TargetType="Border">
            <Setter Property="Background" Value="#D9DEE7"/>
            <Setter Property="Height" Value="5"/>
            <Setter Property="CornerRadius" Value="3"/>
            <Setter Property="Margin" Value="3,0"/>
            <Style.Triggers><DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="True"><Setter Property="Background" Value="#26364A"/></DataTrigger></Style.Triggers>
        </Style>
        <Style x:Key="CompactProgressBar" TargetType="ProgressBar">
            <Setter Property="Height" Value="8"/>
            <Setter Property="Background" Value="#E9EDF2"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid ClipToBounds="True">
                            <Border Background="{TemplateBinding Background}" CornerRadius="4"/>
                            <Border x:Name="PART_Indicator" HorizontalAlignment="Left" Background="{TemplateBinding Foreground}" CornerRadius="4"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="38">
        <Grid Style="{StaticResource ScreenStart}">
            <Border MaxWidth="720" Padding="46" Background="White" CornerRadius="18" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="G-Spiral" FontSize="18" FontWeight="SemiBold" Foreground="#6B7280"/>
                    <TextBlock Text="В какой компании ты работаешь?" FontSize="34" FontWeight="SemiBold" Foreground="#1F2937" Margin="0,12,0,8" TextWrapping="Wrap"/>
                    <TextBlock Text="Ответьте на 7 вопросов о корпоративной культуре. В каждом вопросе можно выбрать любое количество вариантов — от нуля до всех шести." FontSize="16" Foreground="#667085" TextWrapping="Wrap" Margin="0,0,0,28"/>
                    <TextBox Text="{Binding CompanyName, UpdateSourceTrigger=PropertyChanged}" FontSize="18" Padding="14" MinHeight="52" BorderBrush="#C8D0DA" BorderThickness="1" ToolTip="Название компании"/>
                    <Button Content="Начать" Command="{Binding StartCommand}" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Left" Margin="0,24,0,0" IsDefault="True"/>
                </StackPanel>
            </Border>
        </Grid>

        <Grid Style="{StaticResource ScreenQuestion}">
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Margin="4,0,4,18">
                <TextBlock Text="{Binding CompanyName, StringFormat=Корпоративная культура — {0}}" FontSize="16" Foreground="#667085"/>
                <TextBlock Text="{Binding QuestionProgressText}" FontSize="14" FontWeight="SemiBold" Foreground="#6B7280" Margin="0,16,0,4"/>
                <TextBlock Text="{Binding CurrentQuestionTitle}" FontSize="31" FontWeight="SemiBold" Foreground="#1F2937" TextWrapping="Wrap"/>
                <TextBlock Text="Выберите все варианты, которые соответствуют вашей компании. Можно не выбирать ни одного." FontSize="15" Foreground="#667085" Margin="0,7,0,0"/>
            </StackPanel>
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <ItemsControl ItemsSource="{Binding AnswerCards}" Margin="0,0,0,12">
                    <ItemsControl.ItemsPanel><ItemsPanelTemplate><UniformGrid Rows="2" Columns="3"/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ToggleButton IsChecked="{Binding IsSelected, Mode=TwoWay}" Margin="7" Padding="0" Background="{Binding LightHex}" BorderBrush="{Binding PrimaryHex}" BorderThickness="1" Foreground="#303846" FocusVisualStyle="{x:Null}" MinHeight="205">
                                <ToggleButton.InputBindings><KeyBinding Key="Enter" Command="{Binding ToggleCommand}"/></ToggleButton.InputBindings>
                                <ToggleButton.Style>
                                    <Style TargetType="ToggleButton">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Border x:Name="CardBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="13" Padding="20">
                                                        <Grid>
                                                            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                                                            <Grid>
                                                                <TextBlock Text="{Binding TypeName}" FontSize="18" FontWeight="SemiBold" Foreground="{TemplateBinding Foreground}"/>
                                                                <Border HorizontalAlignment="Right" Width="28" Height="28" CornerRadius="14" Background="#44FFFFFF">
                                                                    <TextBlock Text="✓" FontSize="17" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                                        <TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding IsSelected}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></TextBlock.Style>
                                                                    </TextBlock>
                                                                </Border>
                                                            </Grid>
                                                            <TextBlock Grid.Row="1" Text="{Binding Text}" TextWrapping="Wrap" FontSize="14.5" Foreground="{TemplateBinding Foreground}" Margin="0,14,0,0" LineHeight="21"/>
                                                        </Grid>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsChecked" Value="True"><Setter TargetName="CardBorder" Property="Background" Value="{Binding PrimaryHex}"/><Setter Property="Foreground" Value="White"/><Setter TargetName="CardBorder" Property="BorderThickness" Value="2"/></Trigger>
                                                        <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="CardBorder" Property="BorderBrush" Value="#111827"/><Setter TargetName="CardBorder" Property="BorderThickness" Value="3"/></Trigger>
                                                        <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="CardBorder" Property="Opacity" Value="0.94"/></Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ToggleButton.Style>
                            </ToggleButton>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
            <Grid Grid.Row="2" Margin="4,14,4,0">
                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="← Назад" Command="{Binding BackCommand}" Style="{StaticResource SecondaryButton}"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Border Width="52" Style="{StaticResource ProgressSegment}" Tag="{Binding Progress1}"/><Border Width="52" Style="{StaticResource ProgressSegment}" Tag="{Binding Progress2}"/><Border Width="52" Style="{StaticResource ProgressSegment}" Tag="{Binding Progress3}"/><Border Width="52" Style="{StaticResource ProgressSegment}" Tag="{Binding Progress4}"/><Border Width="52" Style="{StaticResource ProgressSegment}" Tag="{Binding Progress5}"/><Border Width="52" Style="{StaticResource ProgressSegment}" Tag="{Binding Progress6}"/><Border Width="52" Style="{StaticResource ProgressSegment}" Tag="{Binding Progress7}"/>
                </StackPanel>
                <Button Grid.Column="2" Content="{Binding NextButtonText}" Command="{Binding NextCommand}" Style="{StaticResource PrimaryButton}"/>
            </Grid>
        </Grid>

        <Grid Style="{StaticResource ScreenResults}">
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Margin="4,0,4,20">
                <TextBlock Text="{Binding CompanyName, StringFormat=Результаты — {0}}" FontSize="31" FontWeight="SemiBold" Foreground="#1F2937"/>
                <TextBlock Text="Типы организации отсортированы по количеству выбранных соответствий." FontSize="15" Foreground="#667085" Margin="0,6,0,0"/>
            </StackPanel>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions><ColumnDefinition Width="1.15*"/><ColumnDefinition Width="0.85*"/></Grid.ColumnDefinitions>
                <Border Grid.Column="0" Background="White" CornerRadius="14" Padding="24" Margin="4,0,12,0">
                    <ItemsControl ItemsSource="{Binding Results}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,8">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="22"/><ColumnDefinition Width="190"/><ColumnDefinition Width="*"/><ColumnDefinition Width="72"/></Grid.ColumnDefinitions>
                                    <Border Width="14" Height="14" CornerRadius="7" Background="{Binding PrimaryHex}" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <ProgressBar Grid.Column="2" Margin="10,0,14,0" Minimum="0" Maximum="7" Value="{Binding Score}" Foreground="{Binding PrimaryHex}" Style="{StaticResource CompactProgressBar}" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="3" Text="{Binding Score, StringFormat={}{0} из 7}" FontSize="14" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="#4B5563"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Border>
                <Border Grid.Column="1" Background="White" CornerRadius="14" Padding="24" Margin="12,0,4,0">
                    <Grid>
                        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                        <TextBlock Text="Профиль корпоративной культуры" FontSize="19" FontWeight="SemiBold"/>
                        <Grid Grid.Row="1" Margin="0,14">
                            <controls:DonutChartControl Items="{Binding Results}" MinHeight="280">
                                <controls:DonutChartControl.Style><Style TargetType="controls:DonutChartControl"><Setter Property="Visibility" Value="Visible"/><Style.Triggers><DataTrigger Binding="{Binding HasSelections}" Value="False"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger></Style.Triggers></Style></controls:DonutChartControl.Style>
                            </controls:DonutChartControl>
                            <TextBlock Text="Нет выбранных соответствий" FontSize="17" Foreground="#6B7280" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding HasSelections}" Value="False"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></TextBlock.Style>
                            </TextBlock>
                        </Grid>
                        <ItemsControl Grid.Row="2" ItemsSource="{Binding Results}">
                            <ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate><DataTemplate><StackPanel Orientation="Horizontal" Margin="0,3,15,3"><Border Width="10" Height="10" CornerRadius="5" Background="{Binding PrimaryHex}" Margin="0,0,6,0"/><TextBlock Text="{Binding Name}" FontSize="12.5" Foreground="#4B5563"/></StackPanel></DataTemplate></ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </Border>
            </Grid>
            <Grid Grid.Row="2" Margin="4,18,4,0">
                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="Сохранить XLSX" Click="SaveReport_Click" Style="{StaticResource PrimaryButton}"/>
                <Button Grid.Column="1" Content="Назад к ответам" Command="{Binding BackToAnswersCommand}" Style="{StaticResource SecondaryButton}" Margin="10,0,0,0"/>
                <Button Grid.Column="3" Content="Пройти заново" Command="{Binding RestartCommand}" Style="{StaticResource SecondaryButton}"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
